name: CI/CD Pipeline to IIS VM

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: 1. Checkout do Repositório
        uses: actions/checkout@v4

      - name: 2. TESTE - Conexao com a VM
        run: |
          Write-Host "================================="
          Write-Host "PIPELINE CHEGOU NA VM!"
          Write-Host "================================="
          hostname
          whoami
          $PSVersionTable.PSVersion
        shell: powershell